export const prerender = true;
// Render HTML during `npm run build`, then deploy only static files to GitHub Pages.
// This does not require (or create) a server at runtime.
export const ssr = true;
export const trailingSlash = 'always';
